import CodeMirrorUt from '../layouts/Content/codemirror';
export declare const getComputedStyleNum: (ele: HTMLElement, key: string) => number;
/**
 * 两块区域同步滚动
 *
 * @param pEle 宿主区域
 * @param cEle 寄主区域
 * @returns 清除监听的方法
 */
export declare const scrollAutoWithScale: (pEle: HTMLElement, cEle: HTMLElement) => (() => void)[];
/**
 * 两块区域同步滚动
 *
 * @param pEle 宿主区域
 * @param cEle 寄主区域
 * @returns 清除监听的方法
 */
declare const scrollAuto: (pEle: HTMLElement, cEle: HTMLElement, codeMirrorUt: CodeMirrorUt) => (() => void)[];
export default scrollAuto;
