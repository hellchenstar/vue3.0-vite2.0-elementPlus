/**
 * 源码来自https://github.com/revin/markdown-it-task-lists
 *
 * 进行了部分重构
 * 1. 使用ts和箭头函数重构
 * 2. 将option作为局部变量，支持了多个实例的使用
 */
import markdownit from 'markdown-it';
interface Options {
    enabled?: boolean;
    label?: boolean;
    labelAfter?: boolean;
}
declare const githubTaskLists: (md: markdownit, options?: Options) => void;
export default githubTaskLists;
